 name: Trigger SampleRepo2

 on:
   workflow_dispatch:
   push:
     branches:
       - main
       
 jobs:
   build:
     name: Run API
     runs-on: ubuntu-latest
     
     steps:
       - name: Build API
         run: |
           echo "Build API"
           sleeps 5s
